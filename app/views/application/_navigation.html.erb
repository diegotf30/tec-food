<nav class="navbar navbar-expand-lg navbar-dark">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
    <span class="navbar-toggler-icon"></span>
  </button>
  <%= link_to "TecFood", root_path, class: 'navbar-brand' %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li id="countries" class="nav-item active <%= 'bold' if current_page?(root_path) %>">
        <div class="nav-link">Home</div>
      </li>
      <li id="reports" class="nav-item active <%= 'bold' if current_page?(new_user_path) %>">
        <%# if current_member_verified? %>
          <%#= link_to "Reportes", reports_path, class: "nav-link"  %>
        <%# else %>
          <div class="nav-link">Restaurantes</div>
        <%# end %>
      </li>
    </ul>
    <%# if current_user %>
      <span class="navbar-text bold">
        <%#= button_to "Salir", destroy_member_session_path, method: :delete, class: "btn btn-danger bold", id: "sign_out" %>
      </span>
    <%# else %>
      <span class="navbar-text bold">
        <%= button_to "Login", new_user_path, method: :get, class: "btn btn-teal bold", id: "sign_in" %>
      </span>
    <%# end %>
  </div>
</nav>


